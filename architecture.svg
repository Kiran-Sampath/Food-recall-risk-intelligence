<svg xmlns="http://www.w3.org/2000/svg" width="800" height="400">
  <rect width="100%" height="100%" fill="#ffffff" />
  <text x="50" y="40" font-size="20" fill="#111">Food Recall Risk Intelligence — Architecture</text>
  <text x="50" y="90" font-size="14">openFDA API → Ingestion (Python) → Bronze (JSON)</text>
  <text x="50" y="120" font-size="14">Bronze → PySpark transforms → Silver → DQ checks → Scoring → Gold</text>
  <text x="50" y="150" font-size="14">Gold → Streamlit Dashboard</text>
</svg>
